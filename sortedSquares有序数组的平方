class Solution 
{
public:
    vector<int> sortedSquares(vector<int>& nums) 
    {
        vector<int> nums1;
        for(int num:nums)
        {
            nums1.push_back(num*num);
        }
        sort(nums1.begin(),nums1.end());
        return nums1;
    }
};
